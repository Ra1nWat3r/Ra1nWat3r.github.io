services:
  jekyll:
    build: .
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - .:/app:z
    environment:
      - JEKYLL_ENV=development
    command: jekyll serve --host 0.0.0.0 --livereload